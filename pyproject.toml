[project]
name = "claude-agentic-ai-scaffolding"
version = "0.1.0"
description = "Nine hands-on lessons for building agents with the Claude Agent SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.152",
    "python-dotenv>=1.2.3",
]
